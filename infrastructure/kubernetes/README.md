# HECATE Kubernetes Configuration
Deploy HECATE to Kubernetes clusters.

## Structure
* `namespaces/`: system isolation
* `rbac/`: security cluster roles
* `serviceaccounts/`: identity per pod
* `deployments/`: platform workloads