apiVersion: apps/v1
kind: Deployment
metadata:
  name: monitoring-agent
  namespace: hecate-agents
spec:
  replicas: 1
  selector:
    matchLabels:
      app: monitoring-agent
  template:
    metadata:
      labels:
        app: monitoring-agent
    spec:
      serviceAccountName: monitoring-agent
      containers:
        - name: monitoring-agent
          image: hecate/monitoring-agent:latest
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"