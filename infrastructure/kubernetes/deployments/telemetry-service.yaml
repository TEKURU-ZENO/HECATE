apiVersion: apps/v1
kind: Deployment
metadata:
  name: telemetry-service
  namespace: hecate-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: telemetry-service
  template:
    metadata:
      labels:
        app: telemetry-service
    spec:
      serviceAccountName: monitoring-agent
      containers:
        - name: telemetry-service
          image: hecate/telemetry-service:latest
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"