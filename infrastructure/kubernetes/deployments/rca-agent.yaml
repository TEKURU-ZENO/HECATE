apiVersion: apps/v1
kind: Deployment
metadata:
  name: rca-agent
  namespace: hecate-agents
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rca-agent
  template:
    metadata:
      labels:
        app: rca-agent
    spec:
      serviceAccountName: monitoring-agent
      containers:
        - name: rca-agent
          image: hecate/rca-agent:latest
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"