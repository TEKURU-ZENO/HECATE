apiVersion: apps/v1
kind: Deployment
metadata:
  name: learning-agent
  namespace: hecate-agents
spec:
  replicas: 1
  selector:
    matchLabels:
      app: learning-agent
  template:
    metadata:
      labels:
        app: learning-agent
    spec:
      serviceAccountName: monitoring-agent
      containers:
        - name: learning-agent
          image: hecate/learning-agent:latest
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"