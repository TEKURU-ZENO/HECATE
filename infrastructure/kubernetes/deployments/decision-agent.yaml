apiVersion: apps/v1
kind: Deployment
metadata:
  name: decision-agent
  namespace: hecate-agents
spec:
  replicas: 1
  selector:
    matchLabels:
      app: decision-agent
  template:
    metadata:
      labels:
        app: decision-agent
    spec:
      serviceAccountName: monitoring-agent
      containers:
        - name: decision-agent
          image: hecate/decision-agent:latest
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"