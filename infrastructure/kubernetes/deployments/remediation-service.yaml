apiVersion: apps/v1
kind: Deployment
metadata:
  name: remediation-service
  namespace: hecate-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: remediation-service
  template:
    metadata:
      labels:
        app: remediation-service
    spec:
      serviceAccountName: monitoring-agent
      containers:
        - name: remediation-service
          image: hecate/remediation-service:latest
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"