apiVersion: apps/v1
kind: Deployment
metadata:
  name: rca-service
  namespace: hecate-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rca-service
  template:
    metadata:
      labels:
        app: rca-service
    spec:
      serviceAccountName: monitoring-agent
      containers:
        - name: rca-service
          image: hecate/rca-service:latest
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"